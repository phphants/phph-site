<?php
declare(strict_types = 1);

/** @var \App\Form\Account\LoginForm $form */

$title = 'Login';
$this->headTitle($title);
?>

</div><!-- end of gradient-wrapper -->

<div class="wrapper content">
    <h1><?php echo $title; ?></h1>

    <?= $this->form()->openTag($form); ?>

    <?= $this->formRow($form->get('email')->setAttributes([
        'placeholder' => 'foo@bar.com',
    ])); ?>

    <?= $this->formRow($form->get('password')); ?>

    <?= $this->formRow($form->get('submit')); ?>

    <?= $this->formRow($form->get('loginForm_csrf')); ?>
    <?= $this->form()->closeTag(); ?>

    <br />

    <h1>Or log in using...</h1>

    <ul class="login-with">
        <li>
            <a href="<?= $this->url('account-twitter-authenticate'); ?>" class="btn btn-block btn-social btn-twitter">
                <span class="fa fa-twitter"></span> Sign in with Twitter
            </a>
        </li>
        <li>
            <a href="<?= $this->url('account-github-authenticate'); ?>" class="btn btn-block btn-social btn-github">
                <span class="fa fa-github"></span> Sign in with GitHub
            </a>
        </li>
    </ul>

    <h1>If you don't have an account...</h1>

    <p>You can <a href="<?= $this->url('account-register'); ?>">register now</a> with your email.</p>

</div>
